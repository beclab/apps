apiVersion: v1
kind: Service
metadata:
  name: ticket-web-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app: ticket
spec:
  selector:
    app: ticket
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
