metadata:
  title: AudioBookShelf
  description: A self-hosted audiobook and podcast server.
spec:
  fullDescription: |
    Audiobookshelf is a self-hosted audiobook and podcast server.

    **Features**
    - Fully open-source, including the android & iOS app (in beta)
    - Stream all audio formats on the fly
    - Search and add podcasts to download episodes w/ auto-download
    - Multi-user support w/ custom permissions
    - Keeps progress per user and syncs across devices
    - Auto-detects library updates, no need to re-scan
    - Upload books and podcasts w/ bulk upload drag and drop folders
    - Backup your metadata + automated daily backups
    - Progressive Web App (PWA)
    - Chromecast support on the web app and android app
    - Fetch metadata and cover art from several sources
    - Chapter editor and chapter lookup (using Audnexus API)
    - Merge your audio files into a single m4b
    - Embed metadata and cover image into your audio files
    - Basic ebook support and ereader
        - Epub, pdf, cbr, cbz
        - Send ebook to device (i.e. Kindle)
    - Open RSS feeds for podcasts and audiobooks
  upgradeDescription: |
    Upgrade Audiobookshelf to 2.36.0 (`advplyr/audiobookshelf:2.36.0`).

    This is a security-focused release: account session management (logout all devices / per-session logout) and fixes for OIDC callback validation, refresh-token use on API/WebSocket (CVE-2026-73085), root account deletion, and bulk library download access checks. Password changes now invalidate existing sessions.

    Full release notes: https://github.com/advplyr/audiobookshelf/releases/tag/v2.36.0
