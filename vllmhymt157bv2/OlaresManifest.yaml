---
olaresManifest.version: '0.11.0'
olaresManifest.type: app
apiVersion: 'v2'
metadata:
  name: vllmhymt157bv2
  icon: https://app.cdn.olares.com/appstore/llm/vllm/other/HY-MT.png
  description: Multilingual translation model from Hunyuan
  appid: vllmhymt157bv2
  title: HY-MT1.5-7B (vLLM)
  version: '1.0.2'
  categories:
    - AI
sharedEntrances:
  - name: vllmhymt157bv2
    host: sharedentrances-api
    post: 0
    title: HY-MT1.5-7B
    invisible: true
    authLevel: internal
    icon: https://app.cdn.olares.com/appstore/llm/vllm/other/HY-MT.png
entrances:
  - authLevel: internal
    host: vllmclient
    icon: https://app.cdn.olares.com/appstore/llm/vllm/other/HY-MT.png
    name: vllmclient
    openMethod: window
    port: 8080
    title: HY-MT1.5-7B

spec:
  versionName: 'tencent/HY-MT1.5-7B'
  fullDescription: |
    ## IMPORTANT NOTE ##
    This is a shared app. Only one service will be installed across the entire Olares system. Every user can install a desktop entry to access or use the shared entrance to call the service API.

    ## OVERVIEW ##
    The Hunyuan-MT comprises a translation model, Hunyuan-MT-7B, and an ensemble model, Hunyuan-MT-Chimera. The translation model is used to translate source text into the target language, while the ensemble model integrates multiple translation outputs to produce a higher-quality result. It primarily supports mutual translation among 33 languages, including five ethnic minority languages in China.


    Key Features and Advantages
    - In the WMT25 competition, the model achieved first place in 30 out of the 31 language categories it participated in.
    - Hunyuan-MT-7B achieves industry-leading performance among models of comparable scale
    - Hunyuan-MT-Chimera-7B is the industry’s first open-source translation ensemble model, elevating translation quality to a new level
    - A comprehensive training framework for translation models has been proposed, spanning from pretrain → cross-lingual pretraining (CPT) → supervised fine-tuning (SFT) → translation enhancement → ensemble refinement, achieving state-of-the-art (SOTA) results for models of similar size

  developer: Tencent
  website: https://hunyuan.tencent.com/
  sourceCode: https://github.com/Tencent-Hunyuan/Hunyuan-MT
  submitter: Olares
  locale:
    - en-US
    - zh-CN
  doc: https://github.com/Tencent-Hunyuan/Hunyuan-MT
  limitedCpu: 21
  requiredCpu: 1
  requiredDisk: 10Gi
  limitedDisk: 50Gi
  limitedMemory: 30Gi
  requiredMemory: 10Gi
  requiredGpu: 16Gi
  limitedGpu: 30Gi
  supportArch:
    - amd64
    - arm64
  subCharts:
  - name: vllmhymt157bv2server
    shared: true
  - name: vllmhymt157bv2
permission:
  appData: true
  appCache: true
  userData:
    - Home
options:
  apiTimeout: 0
  appScope:
  {{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
    clusterScoped: true
    appRef:
      - vllmhymt157bv2
  {{- else }}
    clusterScoped: false
  {{- end }}
  dependencies:
    - name: olares
      version: '>=1.12.3-0'
      type: system
  {{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
  {{- else }}
    - name: vllmhymt157bv2
      type: application
      version: '>=1.0.0'
      mandatory: true
  {{- end }}

{{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
envs:
  - envName: OLARES_USER_HUGGINGFACE_TOKEN
    required: true
    applyOnChange: true
    valueFrom:
      envName: OLARES_USER_HUGGINGFACE_TOKEN
  - envName: OLARES_USER_HUGGINGFACE_SERVICE
    required: true
    applyOnChange: true
    valueFrom:
      envName: OLARES_USER_HUGGINGFACE_SERVICE
  - envName: GPU_MEMORY_UTILIZATION
    required: false
    type: string
    editable: true
    applyOnChange: true
    default: "0.9"
  - envName: MAX_MODEL_LEN
    required: false
    type: string
    editable: true
    applyOnChange: true
    default: "8000"
{{- end }}