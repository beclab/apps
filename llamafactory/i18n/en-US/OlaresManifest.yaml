metadata:
  title: LLaMA Factory
  description: Easily fine-tune 100+ large language models with zero-code CLI and Web UI
spec:
  fullDescription: |
    LlamaFactory, a unified framework that integrates a suite of cutting-edge efficient training methods. It provides a solution for flexibly customizing the fine-tuning of 100+ LLMs without the need for coding through the built-in web UI LlamaBoard.

    **Features**
    - Various models: LLaMA, LLaVA, Mistral, Mixtral-MoE, Qwen, Yi, Gemma, Baichuan, ChatGLM, Phi, etc.
    - Integrated methods: (Continuous) pre-training, (multimodal) supervised fine-tuning, reward modeling, PPO, DPO, KTO, ORPO, etc.
    - Scalable resources: 16-bit full-tuning, freeze-tuning, LoRA and 2/3/4/5/6/8-bit QLoRA via AQLM/AWQ/GPTQ/LLM.int8/HQQ/EETQ.
    - Advanced algorithms: GaLore, BAdam, Adam-mini, DoRA, LongLoRA, LLaMA Pro, Mixture-of-Depths, LoRA+, LoftQ, PiSSA and Agent tuning.
    - Practical tricks: FlashAttention-2, Unsloth, RoPE scaling, NEFTune and rsLoRA.
    - Experiment monitors: LlamaBoard, TensorBoard, Wandb, MLflow, etc.
    - Faster inference: OpenAI-style API, Gradio UI and CLI with vLLM worker.
  upgradeDescription: |
    Storage: durable volumes for `/root`, `/app/data`, and `/app/output` moved from `appCache` to `appData`. On first start after upgrade, initContainers copy legacy appCache contents into appData when each destination is empty (markers: `.migration_from_appcache_done_root`, `.migration_from_appcache_done_app`, `.migration_from_appcache_done_output`). Old appCache data is left intact for rollback.

    Update Chart Config
    - Fix GPU requirements
