# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   comfyuisharev3 — ComfyUI engine / server Deployment (== appid / Release.Name)
workloads:
  comfyuisharev3:
    replicaCount: 1
