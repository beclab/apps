# Olares merges release values on top of these defaults.

backupWebuiDb:
  # Helm-only: when true, enables the backup init container without olaresEnv.
  # On Olares, ENABLE_WEBUI_DB_BACKUP defaults to true in OlaresManifest envs.
  enabled: false
