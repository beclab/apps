metadata:
  title: Open WebUI
  description: User-friendly WebUI for LLMs
spec:
  fullDescription: |
    Open WebUI(Formerly Ollama WebUI) is an extensible, feature-rich, and user-friendly self-hosted WebUI designed to operate entirely offline. It supports various LLM runners, including Ollama and OpenAI-compatible APIs. For more information, be sure to check out our Open WebUI Documentation.

    **Key Features of Open WebUI**
    - Ollama/OpenAI API Integration: Effortlessly integrate OpenAI-compatible APIs for versatile conversations alongside Ollama models. Customize the OpenAI API URL to link with LMStudio, GroqCloud, Mistral, OpenRouter, and more.

    - Pipelines, Open WebUI Plugin Support: Seamlessly integrate custom logic and Python libraries into Open WebUI using Pipelines Plugin Framework. Launch your Pipelines instance, set the OpenAI URL to the Pipelines URL, and explore endless possibilities. Examples include Function Calling, User Rate Limiting to control access, Usage Monitoring with tools like Langfuse, Live Translation with LibreTranslate for multilingual support, Toxic Message Filtering and much more.

    - Responsive Design: Enjoy a seamless experience across Desktop PC, Laptop, and Mobile devices.

    - Progressive Web App (PWA) for Mobile: Enjoy a native app-like experience on your mobile device with our PWA, providing offline access on localhost and a seamless user interface.

    - Full Markdown and LaTeX Support: Elevate your LLM experience with comprehensive Markdown and LaTeX capabilities for enriched interaction.

    - Hands-Free Voice/Video Call: Experience seamless communication with integrated hands-free voice and video call features, allowing for a more dynamic and interactive chat environment.

    - Model Builder: Easily create Ollama models via the Web UI. Create and add custom characters/agents, customize chat elements, and import models effortlessly through Open WebUI Community integration.

    - Native Python Function Calling Tool: Enhance your LLMs with built-in code editor support in the tools workspace. Bring Your Own Function (BYOF) by simply adding your pure Python functions, enabling seamless integration with LLMs.

    - Local RAG Integration: Dive into the future of chat interactions with groundbreaking Retrieval Augmented Generation (RAG) support. This feature seamlessly integrates document interactions into your chat experience. You can load documents directly into the chat or add files to your document library, effortlessly accessing them using the # command before a query.

    - Web Search for RAG: Perform web searches using providers like SearXNG, Google PSE, Brave Search, serpstack, serper, Serply, DuckDuckGo and TavilySearch and inject the results directly into your chat experience.

    - Web Browsing Capability: Seamlessly integrate websites into your chat experience using the # command followed by a URL. This feature allows you to incorporate web content directly into your conversations, enhancing the richness and depth of your interactions.

    - Image Generation Integration: Seamlessly incorporate image generation capabilities using options such as AUTOMATIC1111 API or ComfyUI (local), and OpenAI's DALL-E (external), enriching your chat experience with dynamic visual content.

    - Many Models Conversations: Effortlessly engage with various models simultaneously, harnessing their unique strengths for optimal responses. Enhance your experience by leveraging a diverse set of models in parallel.

    - Role-Based Access Control (RBAC): Ensure secure access with restricted permissions; only authorized individuals can access your Ollama, and exclusive model creation/pulling rights are reserved for administrators.

    - Multilingual Support: Experience Open WebUI in your preferred language with our internationalization (i18n) support. Join us in expanding our supported languages! We're actively seeking contributors!

    - Continuous Updates: We are committed to improving Open WebUI with regular updates, fixes, and new features.
  upgradeDescription: |
    Chart **1.0.43** — Open WebUI **v0.11.1**.
    - **Image:** `docker.io/beclab/open-webui-open-webui:v0.11.1`
    - Stream replies as text deltas instead of resending the whole message on every update (less browser main-thread work on long replies).
    - `ENABLE_ORJSON=true`, `THREAD_POOL_SIZE=256`, `ENABLE_BASE_MODELS_CACHE=true`, `MODELS_CACHE_TTL=300`.
    - Throttle presence writes (`DATABASE_USER_ACTIVE_STATUS_UPDATE_INTERVAL=300`) and WebSocket heartbeats (`WEBSOCKET_HEARTBEAT_INTERVAL=60`).
    - Batch stream tokens (`CHAT_RESPONSE_STREAM_DELTA_CHUNK_SIZE=7`) so the UI is not redrawn on every token.
    - Raise CPU limit from 2 to 4 cores so document ingest and the web UI do not starve each other.

    Chart **1.0.42** (Open WebUI **v0.11.0**).
    - Raise SQLite `busy_timeout` to 30s so WebSocket heartbeats wait out knowledge-ingest writers instead of logging `database is locked`.
    - Seed sidecar polls every 60s (300s when idle) instead of every 10s, and uses the same WAL / busy_timeout pragmas.

    Packaged (Olares chart)
    - **DB backup before upgrade (default on):** **`ENABLE_WEBUI_DB_BACKUP`** defaults to **`true`**. On each new chart version, before the app starts, if `webui.db` exists and `data/backups/webui_chart_<chart-version>.db` is not there yet, the init container copies it once (same appData volume). Set **`false`** under Settings → Application → environment variables **before** upgrading to skip.
    - **Persistent session key (default on):** **`ENABLE_WEBUI_SECRET_KEY_PERSIST`** remains **`true`** via `data/.webui_secret_key` on appData.
    - **Existing data:** Your database under appData is kept; Open WebUI upgrades its schema automatically on startup.

    Open WebUI v0.11.0 → v0.11.1 — highlights
    - Delta streaming, cheaper logging, faster model-list / permission / JSON paths, knowledge search no longer stalls other chats.
    - Security and access-control fixes; update production deployments promptly.
    - Full release notes: https://github.com/open-webui/open-webui/releases/tag/v0.11.1

    Open WebUI v0.10.2 → v0.11.0 — highlights

    **Major features**
    - **Redesigned UI** across chat, admin, menus, and settings (narrower conversation column, lighter typography, rearranged settings).
    - **Sub-agents:** models can hand work to background helper agents (`ENABLE_SUBAGENTS` and related settings).
    - **Folder pages, chat timers, notification targets / assistant notifications,** open link-sharing for chats (admin permission, off by default).
    - **Chat & user variables,** LDAP group sync, shared-folder collaboration, chat fork / compact / previews, customizable keyboard shortcuts.
    - **Files capability** for searching attachments by meaning or text; notes with full chat + attachments; OpenSERP and SearXNG client-cert web search options.
    - **Personal usage dashboard,** memories in settings, import notes/automations, Anthropic passthrough / reasoning / structured output improvements.

    **Security**
    - Security and access-control fixes (terminal HTML preview isolation, math XSS, upload parsing, chat ownership checks, Ollama version auth, and more). Updating production deployments promptly is recommended.
    - See https://github.com/open-webui/open-webui/security

    **Changed**
    - Admin settings moved into the settings window; archived chats under Settings; shorter auto-titles without emojis (prompt editable).
    - Usage token fields: `prompt_tokens` / `completion_tokens` are per latest call; use `input_tokens` / `output_tokens` / `total_tokens` for cumulative billing.
    - `python-jose` and GCS storage emulator no longer bundled in the image.

    **Performance**
    - Broad server-side speedups for chat search, streaming, Redis, model lists, knowledge bases, web search parsing, and filter handling.

    Full release notes: https://github.com/open-webui/open-webui/releases/tag/v0.11.0

