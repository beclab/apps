# Default values for merchant.
# All runtime values (postgres, redis, olaresEnv, user) are injected by the Olares
# installer at deploy time from OlaresManifest.yaml (middleware/envs) and the system context.
