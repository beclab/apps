# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   jinaclipv2embed — embed-server engine Deployment (== appid / Release.Name)
#   llminit           — llm-init download + clipembed proxy
workloads:
  llminit:
    replicaCount: 1
  jinaclipv2embed:
    replicaCount: 1
