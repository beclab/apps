# Currently this sub-chart does not consume any operator-provided values.
# API keys for individual SearXNG engines are exposed as commented placeholders
# directly inside the rendered ConfigMap (searxng-config), so operators who want
# to enable them can `kubectl edit configmap searxng-config` and paste their key
# in-place.  See the inline instructions in templates/searxng-config.yaml.
