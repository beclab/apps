metadata:
  title: RAGFlow
  description: An open-source RAG engine based on deep document understanding.

spec:
  fullDescription: |
    What is RAGFlow?
    RAGFlow is an open-source RAG (Retrieval-Augmented Generation) engine based on deep document understanding. It offers a streamlined RAG workflow for businesses of any scale, combining LLM (Large Language Models) to provide truthful question-answering capabilities, backed by well-founded citations from various complex formatted data.

    **Key Features**
    "Quality in, quality out"
    - Deep document understanding-based knowledge extraction from unstructured data with complicated formats.
    - Finds "needle in a data haystack" of literally unlimited tokens.
    
    Template-based chunking
    - Intelligent and explainable.
    - Plenty of template options to choose from.
    
    Grounded citations with reduced hallucinations
    - Visualization of text chunking to allow human intervention.
    - Quick view of the key references and traceable citations to support grounded answers.
    
    Compatibility with heterogeneous data sources
    - Supports Word, slides, excel, txt, images, scanned copies, structured data, web pages, and more.
    
    Automated and effortless RAG workflow
    - Streamlined RAG orchestration catered to both personal and large businesses.
    - Configurable LLMs as well as embedding models.
    - Multiple recall paired with fused re-ranking.
    - Intuitive APIs for seamless integration with business.

  upgradeDescription: |
    v1.0.30: Upgrade RAGFlow to v0.26.4 (`docker.io/infiniflow/ragflow:v0.26.4`). Companion images unchanged (Infinity v0.7.0, Elasticsearch 8.11.3, MinIO, Valkey).

    Highlights since v0.26.1
    - NLP: language-aware Snowball stemmer (16 languages), Dutch tokenization support.
    - Chat channels (v0.26.2): WhatsApp, DingTalk, WeCom integrations.
    - Parsing (v0.26.3): SoMark OCR for complex layouts; Ingest documents API; batch upload partial-success handling.
    - Fixes (v0.26.4): LM-Studio model names with `@`, MCP list_chats pagination, Docling math formulas, metadata inline edits, bulk file-to-dataset linking, Chinese filter, Laws chunker headings, and more.

    Full release note: https://github.com/infiniflow/ragflow/releases/tag/v0.26.4

    v1.0.29: Move `ELASTIC_PASSWORD`, `REDIS_PASSWORD`, and `MINIO_PASSWORD` into Kubernetes Secret (`secretKeyRef`).

    Storage: durable volumes (ragflow logs, infinity, minio, redis, elasticsearch) moved from `appCache` to `appData` subpaths. On first start after upgrade, initContainers copy legacy appCache contents into appData when destinations are empty (unique `.migration_from_appcache_*_done` markers per volume). Old appCache data is left intact for rollback.

    Configure LLM and embedding models in RAGFlow settings after install or upgrade.
