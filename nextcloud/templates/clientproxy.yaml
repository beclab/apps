apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: nextcloudweb
  name: nextcloudweb
  namespace: '{{ .Release.Namespace }}'
spec:
  replicas: {{ .Values.workloads.nextcloudweb.replicaCount }}
  selector:
    matchLabels:
      io.kompose.service: nextcloudweb
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.network/chrome-default: "true"
        io.kompose.service: nextcloudweb
      annotations:
        checksum/nginx-config: {{ include "nextcloud.nginx.conf" . | sha256sum }}
    spec:
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-config
            defaultMode: 438
            items:
              - key: nginx.conf
                path: nginx.conf
      containers:
        - name: nginx
          image: "docker.io/openresty/openresty:1.29.2.5-bookworm-fat"
          ports:
            - containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            timeoutSeconds: 5
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            timeoutSeconds: 5
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          resources:
            limits:
              cpu: 500m
              memory: 1000Mi
            requests:
              cpu: 10m
              memory: 64Mi
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: nginx.conf
---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud-web
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    io.kompose.service: nextcloudweb
  ports:
    - name: nextcloudweb
      protocol: TCP
      port: 8080
      targetPort: 8080
