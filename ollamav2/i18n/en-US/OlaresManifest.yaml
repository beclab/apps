metadata:
  title: Ollama
  description: Get up and running with large language models.

spec:
  fullDescription: |
    **IMPORTANT NOTE**
    This is a shared app. Once installed by the Olares Admin, all users in the cluster can use it through reference app.

    **OVERVIEW**
    Ollama is a user-friendly interface for running large language models (LLMs) locally. It is a valuable tool for researchers, developers, and anyone who wants to experiment with language models. With Ollama, you can easily download and run LLMs, customize and create your own, and chat with your LLMs using files on your device.

    Ollama supports a wide range of models, including:
    - Llama 3 8B
    - Llama 3 70B
    - Phi 3 Mini 3.8B
    - Phi 3 Medium 14B
    - Gemma 2 9B
    - Gemma 2 27B
    - Mistral 7B
    - Moondream 2 1.4B
    - Neural Chat 7B
    - Starling 7B
    - Code Llama 7B
    - Llama 2 Uncensored 7B
    - LLaVA 7B
    - Solar 10.7B

  upgradeDescription: |
    Upgrade Ollama from v0.30.6 to v0.30.11

    **What's Changed**

    v0.30.11 (2026-06-26)
    - New model: Ornith — a self-improving open-source agentic coding model family (9B / 35B MoE, 256K context, MIT licensed). Run it with `ollama run ornith` (or `ornith:9b` / `ornith:35b`). It runs on this release via the generic chat template; the dedicated Ornith renderer/parser for cleaner agentic tool-call output lands in a later release
    - `ollama launch` now auto-installs opencode and Claude Code when missing, and adds thinking-capability detection for opencode
    - Fixed inverted iGPU/dGPU Vulkan classification on Windows hybrid-graphics machines; Windows now uses the host Vulkan loader
    - Unified and tuned MLX speculative decoding; preserved generation headroom for shifted prompts
    - `ollama ps` no longer double-counts mmap'd weights on partial offload; mmproj offload is now sized by projector memory
    - No new environment variables in this release

    v0.30.6 (2026-06-05)
    - New models: Gemma 4 QAT (Quantization-Aware Training) weights that dramatically cut memory use and boost on-device performance — tags ending in `-qat` (e.g. `gemma4:e2b-it-qat`, `gemma4:12b-it-qat`, `gemma4:31b-it-qat`)
    - `ollama launch omp` now integrates with Oh My Pi, an AI coding agent with IDE integration
    - MLX embedding layers now use NVFP4 global scale for improved quantization on Apple Silicon

    v0.30.5 (2026-06-04)
    - Fixed the `gemma4:12b` floating point exception crash on x86, CUDA, Linux, and Windows
    - `ollama launch hermes-desktop` now skips rebuilding when a packaged desktop app is already installed
    - `ollama launch hermes` now supports native Windows installs through the Hermes PowerShell installer
    - Added Cline CLI integration docs

    v0.30.0 (2026-05-13)
    - Ollama 0.30 improves compatibility and performance using llama.cpp, augmenting the MLX engine on Apple Silicon for broader hardware support
    - Supports a wider range of models, including GGUF-based models from Hugging Face and fine-tuned models
    - Faster performance on NVIDIA hardware
    - Set `LLAMA_ARG_FIT_TARGET=1024` (free VRAM margin per GPU in MiB; must be non-empty — an empty value crashes 0.30 llama-server)
    - Mount CUDA ComputeCache at Home/Ollama/.nv (shared across all Ollama apps) to speed up GPU kernel warmup

    **Known issues**
    - `laguna-xs.2` is not yet supported on Windows/Linux
    - `llama3.2-vision` is not yet supported
    - `nomic-embed-text` now converts inputs to lowercase per the model card (prior versions incorrectly preserved mixed case)

    v0.24.0 (2026-05-14)
    - Ollama 0.24 adds support for the Codex App (OpenAI desktop for Codex threads with worktree and git support); use `ollama launch codex-app` and `ollama launch codex-app --restore` to revert
    - Codex can open local servers in a built-in browser for annotations, and supports review mode for commenting on code in-app
    - Reworked the MLX sampler for improved generation quality on Apple Silicon

    v0.23.2 (2026-05-07)
    - `ollama launch` no longer includes Claude Desktop due to the third-party integration being limited to Anthropic models
    - Use `ollama launch claude-desktop --restore` to restore Claude Desktop to its normal state
    - `/api/show` responses are now cached, improving median latency by ~6.7x which speeds up integrations like VS Code
    - Improved backup workflow when managing launch integrations
    - Cleaner image generation layout in the MLX runner

    For detailed release notes, visit:
    https://github.com/ollama/ollama/releases
