apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: homebox
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  HBOX_DATABASE_PASSWORD: {{ .Values.postgres.password | quote }}
