apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: leantime
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  LEAN_DB_PASSWORD: {{ .Values.mysql.password | quote }}
