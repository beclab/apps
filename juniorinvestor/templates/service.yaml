apiVersion: v1
kind: Service
metadata:
  name: juniorinvestor
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: juniorinvestor
  ports:
    - name: http
      port: 8000
      targetPort: 8000
