apiVersion: v1
kind: Service
metadata:
  name: jupyternotebook
  namespace: {{ .Release.Namespace }}
  labels:
    app: jupyternotebook
spec:
  selector:
    app: jupyternotebook
  ports:
  - name: http
    protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 8888
  type: ClusterIP
