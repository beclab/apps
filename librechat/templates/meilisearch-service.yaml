---
apiVersion: v1
kind: Service
metadata:
  name: librechat-meilisearch
  namespace: '{{ .Release.Namespace }}'
  labels:
    app: librechat
    component: meilisearch
spec:
  selector:
    app: librechat
    component: meilisearch
  ports:
  - name: meilisearch
    port: 7700
    targetPort: 7700
  type: ClusterIP

