image:
  version: '1.1.0'

userspace:
  appdata: ""

postgres:
  host: local
  port: 5432
  password: default
  username: refly
  databases:
    refly: refly

# MinIO configuration
# Note: These values may be automatically injected by the Olares platform
# If deploying standalone, ensure these values are set appropriately
minio:
  host: minio-svc
  port: 9000
  username: refly
  password: refly
  buckets:
    external: external
    internal: internal
