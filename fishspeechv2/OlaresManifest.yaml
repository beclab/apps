---
olaresManifest.version: '0.11.0'
olaresManifest.type: app
apiVersion: 'v2'
metadata:
  name: fishspeechv2
  icon: https://app.cdn.olares.com/appstore/fishspeech/icon.png
  description: SOTA Open Source TTS
  appid: fishspeechv2
  title: Fish Speech
  version: '1.0.5'
  categories:
  - AI
sharedEntrances:
- name: fishspeechv2
  title: Fish Speech
  host: sharedentrances-api
  icon: https://app.cdn.olares.com/appstore/fishspeech/icon.png
  port: 0
  invisible: true
  authLevel: internal
entrances:
- authLevel: internal
  host: fishspeechclient
  icon: https://app.cdn.olares.com/appstore/fishspeech/icon.png
  name: fishspeechclient
  openMethod: window
  port: 8080
  title: Fish Speech
spec:
  versionName: 'webui-cuda-cu129-20260422'
  fullDescription: |
    ## IMPORTANT NOTE ##
    This is a shared app. Only one service will be installed across the entire Olares system. Every user can install a desktop entry to access or use the shared entrance to call the service API.

    ## OVERVIEW ##
    True human-like Text-to-Speech and Voice Cloning

    Bundled checkpoint: [fishaudio/s2-pro](https://huggingface.co/fishaudio/s2-pro) — Fish Audio's latest pro-tier release of the expressive TTS / voice-cloning family.

    FishAudio-S2 Pro is an expressive text-to-speech (TTS) and voice cloning model developed by Fish Audio, designed to generate speech that sounds natural, realistic, and emotionally rich — not robotic, not flat, and not constrained to studio-style narration.

    FishAudio-S2 Pro focuses on how humans actually speak: with emotion, variation, pauses, and intent.

    Features
    - Zero-shot & Few-shot TTS: Input a 10 to 30-second vocal sample to generate high-quality TTS output.

    - Multilingual & Cross-lingual Support: Simply copy and paste multilingual text into the input box—no need to worry about the language. Currently supports English, Japanese, Korean, Chinese, French, German, Arabic, and Spanish.

    - No Phoneme Dependency: The model has strong generalization capabilities and does not rely on phonemes for TTS. It can handle text in any language script.

    - Highly Accurate: Achieves a low CER (Character Error Rate) of around 0.4% and WER (Word Error Rate) of around 0.8% for Seed-TTS Eval.

    - Fast: Accelerated by torch compile, the real-time factor is approximately 1:7 on an Nvidia RTX 4090 GPU.

    - WebUI Inference: Features an easy-to-use, Gradio-based web UI compatible with Chrome, Firefox, Edge, and other browsers.

    - GUI Inference: Offers a PyQt6 graphical interface that works seamlessly with the API server. Supports Linux, Windows, and macOS.

  upgradeDescription: |
    - feat(model): switch bundled checkpoint to fishaudio/s2-pro (latest pro-tier expressive TTS / voice-cloning release).
    - chore(runtime): the fishspeech container's command now polls for the s2-pro download Job's `.s2-pro.done` marker before launching the WebUI, so it no longer starts before the weights are on disk.
    - fix(cuda): bypass `uv run` at container start and launch `run_webui.py` directly with `/app/.venv/bin/python` (plus `UV_NO_SYNC=1` / `UV_FROZEN=1`). Previously the image's `start_webui.sh` used `uv run`, which performed an implicit `uv sync` without `--extra cu129` at runtime and silently downgraded torch to cu126 — Blackwell (sm_120) then failed with "no kernel image is available for execution on the device". The baked cu129 venv is now preserved as-is.
    - fix(docker): bump CUDA 12.6 → 12.9 for Blackwell (sm_120) support (#1225). CUDA 12.6 / cu126 fails with "no kernel image available" on Blackwell GPUs (RTX 5000 series, sm_120); upgrading to CUDA 12.9 / cu129 fixes this. CUDA_VER and UV_EXTRA are exposed as overridable build args in compose.base.yml so users on older drivers can still pass CUDA_VER=12.6.0 UV_EXTRA=cu126 to restore the previous behaviour.
  developer: Fish Audio
  website: https://speech.fish.audio/
  sourceCode: https://github.com/fishaudio/fish-speech
  submitter: Olares
  locale:
  - en-US
  - zh-CN
  doc: https://speech.fish.audio/
  license:
  - text: Apache-2.0
    url: https://github.com/fishaudio/fish-speech/blob/main/LICENSE
  {{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
  limitedCpu: 6
  requiredCpu: 1
  requiredDisk: 50Mi
  limitedDisk: 50Gi
  limitedMemory: 40Gi
  requiredMemory: 16Gi
  requiredGpu: 4Gi
  limitedGpu: 22Gi
  {{- else }}
  requiredMemory: 64Mi
  limitedMemory: 256Mi
  requiredDisk: 1Mi
  limitedDisk: 100Mi
  requiredCpu: 10m
  limitedCpu: 100m
  {{- end }}
  supportArch:
  - amd64
  subCharts:
  - name: fishspeechv2server
    shared: true
  - name: fishspeechv2
permission:
  appData: true
  appCache: true
  userData:
    - Home
options:
  apiTimeout: 0
  dependencies:
  - name: olares
    version: '>=1.12.3-0'
    type: system
{{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
{{- else }}
  - name: fishspeechv2
    type: application
    version: '>=1.0.0'
    mandatory: true
{{- end }}
  appScope:
{{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
    clusterScoped: true
    appRef:
    - fishspeechv2
{{- else }}
    clusterScoped: false
{{- end }}

{{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
envs:
  - envName: OLARES_USER_HUGGINGFACE_TOKEN
    required: true
    applyOnChange: true
    valueFrom:
      envName: OLARES_USER_HUGGINGFACE_TOKEN
  - envName: OLARES_USER_HUGGINGFACE_SERVICE
    required: true
    applyOnChange: true
    valueFrom:
      envName: OLARES_USER_HUGGINGFACE_SERVICE
{{- end }}
