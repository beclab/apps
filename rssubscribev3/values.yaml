# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   rssubscribev3 — rss-server + rssubscribe Deployment (must match metadata.name / appid)
workloads:
  rssubscribev3:
    replicaCount: 1
