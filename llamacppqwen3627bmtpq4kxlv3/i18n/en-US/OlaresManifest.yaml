metadata:
  description: "Qwen3.6-27B MTP UD-Q4_K_XL served with llama.cpp."
  title: Qwen3.6-27B MTP (llama.cpp)

spec:
  upgradeDescription: |
    v1.0.23: Model Console (llm-init) updated to v1.7.21.

    v1.0.22: Model Console upgrades to llm-init v1.7.19.

    v1.0.21: Budget host memory for the GPU memory that time-slicing evicts into RAM (engine limit 30Gi→38Gi, request 6Gi→7Gi, limitedMemory 32Gi→40Gi, requiredMemory 7Gi→8Gi). The old limit was already fully consumed, which cost the container an OOM kill whenever another model took the card. GPU quota unchanged.

    v1.0.15: Lower the NVIDIA host-memory reservation so 1.12.6 timeslice nodes can install more easily (engine request 21Gi→6Gi, chart requiredMemory 22Gi→7Gi). GPU quota unchanged.

    v1.0.11: bump llm-init to v1.3.8.
    v1.0.10: bump llama.cpp to `server-cuda12-b10068`; HF_TOKEN via Secret + OLARES_SYSTEM_HUGGINGFACE_*.

    v1.0.9: Move `HF_TOKEN` into Kubernetes Secret (`secretKeyRef`); map HF env from `OLARES_SYSTEM_HUGGINGFACE_*`.

    v1.0.8: LLAMACPP_MEMORY_LIMIT 30Gi; llm-init limit 2Gi; limitedMemory 32Gi (30+2).
    v1.0.7: nvidia host memory — requiredMemory 22Gi, LLAMACPP_MEMORY_REQUEST 21Gi, LLAMACPP_MEMORY_LIMIT 26Gi, limitedMemory 30Gi.
    v1.0.6: nvidia host memory — requiredMemory 16Gi, LLAMACPP_MEMORY_REQUEST 15Gi, LLAMACPP_MEMORY_LIMIT 19Gi, limitedMemory 26Gi.
    v1.0.5: bump engine args to -c 131072.
    v1.0.5: align nvidia and nvidia-gb10 requiredCpu=1300m.
    v1.0.4: bump llama.cpp to server-cuda12-b10068.
    v1.0.4: bump llm-init to v1.3.1.
    v1.0.4: align nvidia requiredMemory=12Gi and LLAMACPP_MEMORY_REQUEST=11Gi (qwopus pattern).
  fullDescription: |
    **Model Overview**
    Qwen3.6-27B MTP is a text model variant with built-in Multi-Token Prediction (MTP) heads from the Qwen series (Alibaba). MTP draft decoding predicts multiple tokens per step for higher generation throughput. It supports thinking and tool calling. This app serves the `UD-Q4_K_XL` GGUF build from `unsloth/Qwen3.6-27B-MTP-GGUF` via llama.cpp (text-only preset, no vision/mmproj).

    Recommended when you need very high token generation speed. Models with MTP technology accelerate decode by drafting multiple tokens in each step.

    **Preset Configuration**
    - Model: `unsloth/Qwen3.6-27B-MTP-GGUF:UD-Q4_K_XL`
    - Capabilities: thinking, tools
    - Engine args: `-c 100352 -ngl all -fa on -ctk q8_0 -ctv q8_0 --jinja -np 1 --spec-type draft-mtp --spec-draft-n-max 2` (98K context, MTP draft-2, full GPU offload, q8_0 KV cache)
    - MTP adds modest VRAM overhead; if OOM at full 98K context, reduce `-c` (e.g. `-c 65536`).
    - Required GPU memory: `23Gi`

    **Accelerator modes**
    | Mode           | Device            | Details                                          |
    |----------------|-------------------|--------------------------------------------------|
    | `nvidia`       | GPU (CUDA)        | Uses `23Gi` GPU memory; `-ngl all` for full GPU  |
    | `nvidia-gb10`  | GPU (Spark)       | Like above, but memory is managed by the pod     |
    
    **How It Works**
    First, Model Console downloads the model and gets it ready, then `llama.cpp` starts serving when ready. MTP (`--spec-type draft-mtp`) uses the model's built-in draft head to predict multiple tokens per step for higher decode throughput.

    **Stack**
    - **llama.cpp**: Runs the model and serves API requests (port 8081)
    - **Model Console**: Downloads the model, shows progress, and provides the OpenAI API on port 8090, proxying to `llamacpp`

    **Model Storage**
    All model files are stored in a shared folder at `appCommon/huggingface`, mounted as `/cache/hf/hub`. All similar apps use this location for models.

