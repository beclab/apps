metadata:
  title: Hermes Agent
  description: The agent that grows with you
spec:
  fullDescription: |
    **The self-improving AI agent built by [Nous Research](https://nousresearch.com).** It's the only agent with a built-in learning loop — it creates skills from experience, improves them during use, nudges itself to persist knowledge, searches its own past conversations, and builds a deepening model of who you are across sessions. Run it on a $5 VPS, a GPU cluster, or serverless infrastructure that costs nearly nothing when idle. It's not tied to your laptop — talk to it from Telegram while it works on a cloud VM.

    **Key Features**
    - **A real terminal interface**: Full TUI with multiline editing, slash-command autocomplete, conversation history, interrupt-and-redirect, and streaming tool output.
    - **Lives where you do**: Telegram, Discord, Slack, WhatsApp, Signal, and CLI — all from a single gateway process. Voice memo transcription, cross-platform conversation continuity.
    - **A closed learning loop**: Agent-curated memory with periodic nudges. Autonomous skill creation after complex tasks. Skills self-improve during use. FTS5 session search with LLM summarization for cross-session recall. [Honcho](https://github.com/plastic-labs/honcho) dialectic user modeling. Compatible with the [agentskills.io](https://agentskills.io) open standard.
    - **Scheduled automations**: Built-in cron scheduler with delivery to any platform. Daily reports, nightly backups, weekly audits — all in natural language, running unattended.
    - **Delegates and parallelizes**: Spawn isolated subagents for parallel workstreams. Write Python scripts that call tools via RPC, collapsing multi-step pipelines into zero-context-cost turns.
    - **Runs anywhere, not just your laptop**: Six terminal backends — local, Docker, SSH, Singularity, Modal, and Daytona. Daytona and Modal offer serverless persistence — your agent's environment hibernates when idle and wakes on demand, costing nearly nothing between sessions. Run it on a $5 VPS or a GPU cluster.
    - **Research-ready**: Batch trajectory generation, trajectory compression for training the next generation of tool-calling models.

  upgradeDescription: |
    Upstream release: [Hermes Agent v0.21.0 (v2026.8.31)](https://github.com/NousResearch/hermes-agent/releases/tag/v2026.8.31)

    **What's changed**
    - Bot Mode is now built in, with named agents, avatars, group rooms, @-mentions, and durable bot-to-bot conversations through `hermes peer`.
    - Scheduled jobs can retain memory and continuity across runs, use durable notepads, and skip monitor runs when nothing changed.
    - Running subagents can now be listed, redirected, or stopped, with optional JSON Schema validation and per-task cost reporting.
    - The CLI adds a Ctrl+P command palette, fuzzy `/model` search, richer `/status` details, live performance metrics, session pinning, and a global emergency stop.
    - New providers include Meta Model API, CommandCode, Tencent TokenPlan, Nebius Token Factory, Ramp Router, and Actual Computer.

    **Fixes**
    - Improved recovery from stalled providers, structured-output failures, interrupted commands, session database contention, and repeated compaction failures.
    - Improved Gateway and channel reliability, including final-message delivery, Slack streaming, relay voice transcription, and reconnect event replay.
    - Protected agent instructions, skills, and memory files now require write approval, with broader secret redaction and destructive-command checks.

    **Configuration and behavior changes**
    - Delegation defaults increased to 250 iterations and 10 concurrent subagents; review resource and provider limits for heavily parallel workflows.
    - The new `model_overrides` setting can adjust a model's context window, pricing, or capabilities without waiting for a release.
