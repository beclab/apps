apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: ghost
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  database__connection__password: {{ .Values.mysql.password | quote }}
